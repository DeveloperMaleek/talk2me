class User {
  int id;
  String firstName;
  String lastName;
  String profileImage;
  String status;
  String bio;
  String email;
  // Timestamp creationDate;

  User(this.id, this.firstName, this.lastName, this.profileImage, this.status,
      this.bio, this.email);
}
