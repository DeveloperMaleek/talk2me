import 'package:flutter/material.dart';

class Settings_Demo extends StatefulWidget {
  const Settings_Demo({Key? key}) : super(key: key);

  @override
  State<Settings_Demo> createState() => _Settings_DemoState();
}

class _Settings_DemoState extends State<Settings_Demo> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
